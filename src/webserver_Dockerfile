FROM python:3.7.7-buster

RUN pip install flask
RUN pip install mysql-connector-python==8.0.18

COPY /webserver /usr/bin/dtd
RUN chmod +x /usr/bin/dtd/run.sh

COPY /config.json /usr/bin/dtd

WORKDIR /usr/bin/dtd
ENTRYPOINT ./run.sh
